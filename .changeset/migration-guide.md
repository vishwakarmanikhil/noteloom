---
'noteloom': patch
---

Add `docs/migration.md` — a 0.3.x → 0.5.0 upgrade guide covering the new subpath
imports, the `defineBlock` / `extensions` API, and the document format, plus what
is slated for removal in a future 2.0.
